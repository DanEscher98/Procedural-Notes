module FunnyAlgos

greet() = print("Hello World!")

"""
# Finding the percentage of prime numbers

> Let `$p(n)$` be the number of primes between `[0, n)`, the hypotesis is that the limit
> `\$\lim_{n \to \infty} \frac{p(n)}{n}$` exists and is different from $0$.

```
function filter_p(p: Int, naturals: [Int]) -> [Int]:
  return naturals.filter(|n| n%p != 0)
end

MAX = 100_000_000
naturals = range(2, MAX)
idx = 0

while n in 2:MAX
  idx += 1
```

"""


end
